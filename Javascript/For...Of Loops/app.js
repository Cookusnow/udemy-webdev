const chaosEmeralds = ['red', 'yellow', 'white', 'blue', 'cyan', 'green', 'purple'];

for (let i = 0; i < chaosEmeralds.length; i++) {
    console.log(`Where is that damn ${chaosEmeralds[i]} Chaos Emerald`);
}

for (let color of chaosEmeralds) {
    console.log(`Where is that damn ${color} Chaos Emerald`);
}